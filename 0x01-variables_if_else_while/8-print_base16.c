#include <unistd.h>
#include <stdio.h>
/**
 * main - Entry point
 * Description: prints lowercase alphabets in reverse
 * Return: Always 0 (succsess)
 */
int main(void)
{char a = 0;
	do{
		putchar(a);
	}
	while (a++ < 9);
	a = b;
	do {
		putchar(a);
	}
	while (a++ < f);
	putchar(a);
}
while(a++ <f);
putchar(\n);
return 0;
}

