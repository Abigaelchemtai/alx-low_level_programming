#include "main.h"

/**
 * add - that adds two integer and returns the result
 * @a: int that adds to b
 * @b: int that adds to a
 * Return: Sum of a and b
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
